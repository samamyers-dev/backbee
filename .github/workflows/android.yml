name: Build

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  # Not cancel-in-progress: a manual dispatch while a push build is running
  # would kill the push build, and two dead runs look exactly like one slow one.
  cancel-in-progress: false

permissions:
  # write: the screenshots step force-pushes its orphan branch.
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: temurin

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          # The action's default package list is "tools platform-tools", and
          # "tools" was retired from the SDK repository: asking for it makes
          # sdkmanager exit 1 and the whole job dies before anything compiles.
          # platform-tools is all this build needs installed by hand; Gradle
          # fetches the platform and build-tools it wants on its own.
          packages: platform-tools

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # settings.gradle.kts skips :app when it cannot find an SDK, so without
      # this guard a misconfigured runner would report a green build that
      # quietly produced no APK at all.
      - name: Verify the Android SDK is visible to Gradle
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          test -d "$ANDROID_HOME" || { echo "::error::No Android SDK on the runner"; exit 1; }
          ./gradlew projects --console=plain | tee /tmp/projects.txt
          grep -q "Project ':app'" /tmp/projects.txt \
            || { echo "::error::':app' was excluded from the build"; exit 1; }

      - name: Core tests (pure JVM)
        run: ./gradlew :core:test --console=plain --stacktrace

      - name: Assemble debug APK
        run: ./gradlew :app:assembleDebug --console=plain --stacktrace

      # Published straight after assembling, before the tests. A failing test
      # should turn the build red, but it should not withhold an APK that
      # compiled - the artifact is how the app gets onto a phone at all.
      # Room writes the schema JSON for the current @Database version during
      # the build. If that differs from what is committed, an entity changed
      # without a version bump and a Migration - which would crash every
      # existing install at first open. Fail here instead.
      - name: Check the Room schema is committed
        run: |
          if [ -n "$(git status --porcelain app/schemas)" ]; then
            git --no-pager diff app/schemas
            git status --porcelain app/schemas
            echo "::error::app/schemas changed during the build. Bump the Room version, add a Migration, and commit the new schema JSON."
            exit 1
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: backbee-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 30

      - name: App unit tests
        run: ./gradlew :app:testDebugUnitTest --console=plain --stacktrace

      # The release build type is what ships: R8 shrinking, resource shrinking
      # and lintVital all run only here. Building it on every push - unsigned,
      # no keystore on this workflow - is what stops a keep-rule gap or a
      # fatal lint issue from first appearing on the day of a release.
      - name: Assemble release (unsigned) and bundle
        run: ./gradlew :app:assembleRelease :app:bundleRelease --console=plain --stacktrace

      # An installable copy of the release build. Android refuses to install an
      # unsigned APK, so the shrunk release artifact cannot be driven on a
      # phone as-is, and the upload key that would sign it properly lives only
      # in the Release workflow's secrets. Signing it here with the throwaway
      # debug key gives a build where R8, resource shrinking, the release
      # manifest and the release application id are all exactly what ships and
      # only the signature differs.
      #
      # Because the signature differs it cannot be upgraded in place to a Play
      # build later: that install has to be removed first.
      - name: Sign the release APK for on-device testing
        run: |
          set -euo pipefail
          mkdir -p dist
          apk=$(ls app/build/outputs/apk/release/*.apk | head -1)
          keystore="$HOME/.android/debug.keystore"
          if [ ! -f "$keystore" ]; then
            mkdir -p "$(dirname "$keystore")"
            keytool -genkeypair -v -keystore "$keystore" \
              -storepass android -keypass android -alias androiddebugkey \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US"
          fi
          apksigner=$(ls -d "$ANDROID_HOME"/build-tools/*/apksigner | sort -V | tail -1)
          "$apksigner" sign --ks "$keystore" --ks-pass pass:android \
            --key-pass pass:android --out dist/backbee-release-testkey.apk "$apk"
          "$apksigner" verify --print-certs dist/backbee-release-testkey.apk
          ls -lh dist/backbee-release-testkey.apk

      - name: Upload release build outputs
        uses: actions/upload-artifact@v4
        with:
          name: backbee-release-installable
          path: |
            dist/backbee-release-testkey.apk
            app/build/outputs/bundle/release/*.aab
            app/build/outputs/mapping/release/mapping.txt
          if-no-files-found: error
          retention-days: 14

      - name: Lint
        if: always()
        run: ./gradlew :app:lintDebug --console=plain || true

      # Rendered screens, published to their own branch. Artifact downloads are
      # signed blob URLs that are not reachable from every environment, whereas
      # a branch can simply be fetched - and the images stay reviewable in the
      # repository rather than expiring in 30 days.
      - name: Publish screenshots
        if: always()
        run: |
          shopt -s nullglob
          shots=(app/build/outputs/roborazzi/*.png)
          if [ ${#shots[@]} -eq 0 ]; then
            echo "No screenshots produced; nothing to publish."
            exit 0
          fi
          echo "Publishing ${#shots[@]} screenshot(s)."

          tmp=$(mktemp -d)
          cp "${shots[@]}" "$tmp/"

          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Orphan branch: screenshots only, no code history, force-pushed each
          # run so it always reflects the current commit rather than accreting.
          git checkout --orphan screenshots
          git rm -rf --cached . >/dev/null 2>&1 || true
          rm -rf $(git ls-files) 2>/dev/null || true
          mkdir -p screenshots
          cp "$tmp"/*.png screenshots/
          {
            echo "# Rendered screens"
            echo
            echo "Commit: ${GITHUB_SHA}"
            echo "Run: ${GITHUB_RUN_ID}"
            echo
            for f in screenshots/*.png; do echo "- \`$f\`"; done
          } > README.md
          git add -A screenshots README.md
          git commit -q -m "Screens rendered from ${GITHUB_SHA::7}" || exit 0
          git push -f origin screenshots

      - name: Upload test and lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            core/build/reports/tests/**
            app/build/reports/**
          if-no-files-found: ignore
          retention-days: 14
